package io.boodskap.iot.wb;

public enum TreeMenuType {
	ROOT_MENU,
	DOMAIN_RULE,
	MESSAGE_RULES,
	NAMED_RULES,
	SCHEDULED_RULES,
	MESSAGE_RULE,
	NAMED_RULE,
	SCHEDULED_RULE,
	JAR_FILES,
	GROOVY_CLASSES,
	JAR_FILE,
	GROOVY_CLASS,
}
